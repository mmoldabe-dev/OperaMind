## OperaMind - пример файла окружения
# Скопируйте этот файл в .env и задайте реальные значения.

# --- Аналитика (Gemini) ---
# Ключ Google Gemini (обязателен для анализа разговора)
GEMINI_API_KEY=your_gemini_api_key_here

# --- Flask ---
# Секретный ключ Flask (измените для prod)
SECRET_KEY=operamind-secret-key-change-me

# --- Vosk (офлайн распознавание речи) ---
# Путь к распакованной модели Vosk (например vosk-model-small-ru-0.22)
# Если оставить пустым — будет использоваться Google SpeechRecognition
VOSK_MODEL_PATH=/absolute/path/to/vosk-model-small-ru-0.22

# Порог паузы (секунды) для разделения сегментов (используется только при Vosk)
PAUSE_THRESHOLD=0.8

# Включение кластеризации (1) или отключение (0) (MFCC + KMeans) спикеров
# Если =0, роли будут определяться простым чередованием
SPEAKER_CLUSTERING=1

# --- Опционально: OpenAI Whisper (планируемая интеграция) ---
# OPENAI_API_KEY=your_openai_api_key_here

# Примечание: не коммитьте файл .env в репозиторий.